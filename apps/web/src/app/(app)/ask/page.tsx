import { ComingSoon } from "@/components/coming-soon";

export default function AskPage() {
  return (
    <ComingSoon
      title="Ask MomentLog"
      note="Semantic search over your Moments arrives in Phase 5, once the AI worker is embedding entries in the background."
    />
  );
}
