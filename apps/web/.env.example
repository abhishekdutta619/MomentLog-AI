# Postgres connection string (local Docker, or Neon/Supabase once chosen)
DATABASE_URL=

# Generate with: openssl rand -base64 32
AUTH_SECRET=

# Used by NextAuth for callback URLs in dev
NEXTAUTH_URL=http://localhost:3000

# Added in later phases — not needed for Phase 1
# AI_PROVIDER_API_KEY=
# WEATHER_API_KEY=
# R2_ACCESS_KEY_ID=
# R2_SECRET_ACCESS_KEY=
# R2_BUCKET=
